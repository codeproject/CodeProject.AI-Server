{
  "ModuleOptions": {

    /* We may want to store modules outside of the installation, but we don't
       need to, plus it potentially means permission issues. For Docker we just
       map paths, so no drama there.
       
    "DownloadedModulePackagesPath": "/opt/CodeProject/AI/downloads/modules",
    "ModulesPath":                  "/opt/CodeProject/AI/modules",
    */

    "PythonRelativeInterpreterPath": "/bin/%OS%/%PYTHON_DIRECTORY%/venv/bin/python3" // ** %PYTHON_PATH% will expand to this value
  }
}
