{
  "ModuleOptions": {

    /* In 'production' Linux we'd do this, but 'production' in Linux, at
       this stage, means Docker. For Docker we will be mapping image folders
       to host folders, and everything in Docker runs as root, so we'll keep
       things simple and specify a mapping (if needed) in the Docker run
       command to organise things.

    "DownloadedModulePackagesPath": "/opt/CodeProject/AI/downloads/modules",
    "ModulesPath":                  "/opt/CodeProject/AI/modules",
    */
    "PythonRelativeInterpreterPath": "/bin/%OS%/%PYTHON_RUNTIME%/venv/bin/python3" // ** %PYTHON_PATH% will expand to this value
  }
}
